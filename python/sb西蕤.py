weather=['Weather condition','weather','Atmospheric conditions','The elements']
time=['time','Moment','Period']
while True:
    a=0
    s=str(input('撒比，說話:'))
    ss=s.split('.')
    for i in range(len(ss)):
        for p in range(len(weather)):
            if ss[i]==
            if ss[i] in weather[p]:
                print('天氣很晴朗呢!')
                a=1
            if a==1:
                break
        for p in range(len(time)):
            if ss[i] in time[p]:
                print('你沒時鐘嗎?')
                a=1
            if a==1:
                break
        if a==1:
            break
